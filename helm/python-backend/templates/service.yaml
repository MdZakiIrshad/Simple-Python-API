apiVersion: v1
kind: Service
metadata:
  name: {{ .Release.Name }}-python-backend-service
spec:
  type: {{ .Values.service.type }}
  selector:
    app: {{ .Release.Name }}-python-backend
  ports:
    - protocol: TCP
      port: {{ .Values.service.port }}
      targetPort: 8000
